import styled from "styled-components";

const Container = styled.div``;

const Wrapper = styled.div``;

const Card = () => {
  return (
    <Container>
      <Wrapper>This is a Card</Wrapper>
    </Container>
  );
};

export default Card;
